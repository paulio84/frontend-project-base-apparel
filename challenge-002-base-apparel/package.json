{
  "name": "base-apparel",
  "version": "1.0.0",
  "description": "A coming soon page for base apparel.",
  "main": "index.js",
  "keywords": [],
  "author": "Paul Terry",
  "license": "ISC",
  "scripts": {
    "clean": "rm -rf dist/",
    "copyfiles": "npm run clean && mkdir dist && cp src/assets/images/icon-error.svg dist/icon-error.svg",
    "start": "npm run copyfiles && parcel src/index.html -p 3000",
    "build-prod": "npm run copyfiles && parcel build src/index.html"
  },
  "dependencies": {},
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.3"
  }
}